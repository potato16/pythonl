''' There exists exaclty one Pythagorean triplet for which a+b+c=1000.'''
import math
product=0
for a in range(1,1000):
	for b in range(a+1,1000):
		c = math.sqrt(a**2+b**2)
		if c.is_integer()&(a+b+c==1000):
			product = int(a*b*c)
print(product)
			
			
